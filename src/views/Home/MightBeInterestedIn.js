import React, { memo } from 'react';
import PropTypes from 'prop-types';
import styled from 'styled-components';

const MightBeInterestedIn = memo(props => {
  return <div />;
});

MightBeInterestedIn.propTypes = {};

export default MightBeInterestedIn;
